server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: application
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    service-url:
      defaultZone: http://user:123456@localhost:8761/eureka/
